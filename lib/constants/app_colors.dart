import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBackground = Color(0xFF004D40);
  static const Color cardBackground = Color(0xFF003D33);
  static const Color cardBorder = Color(0xFF0D5D52);
  static const Color accent = Colors.amber;
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}